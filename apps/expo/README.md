# Expo

This is the React Native app inside the Bun/Turborepo workspace.

Run it from the repo root:

- `bun install`
- `bun run dev:expo`

Native entrypoints stay under this folder:

- `android/`
- `ios/`
- `src/`
